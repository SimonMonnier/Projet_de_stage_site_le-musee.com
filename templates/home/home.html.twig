{% extends 'base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block body %}

<div class="container mt-3 shadow-lg p-0 mb-5 bg-primary rounded">
    {# <div id="carouselExampleSlidesOnly" class="carousel slide carousel-fade" data-interval="3000" data-pause="false" data-ride="carousel">
        <div class="carousel-inner">
        {% for voiture in voitures %}
            {% set url = path('voitures_show',{'slug': voiture.slug}) %}
                <div class="carousel-item {% if loop.first %} active {% endif %}">
                    <a href="{{ url }}" target="_blank">
                        <img src="/uploads/{{voiture.coverImage}}" class="img-fluid" style="height: 500px; width: 100%; display: block" alt="Responsive image" alt="...">
                    </a>
                    <div class="carousel-caption d-none d-md-block">
                        <a href="{{ url }}" target="_blank">
                            <h1 class="voiture-title">{{ voiture.marque }} {{ voiture.modele }} {% if voiture.type %} {{ voiture.type }} {% endif %}</h1>
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div> #}
        <video width="100%" height="500px" autoplay="true" loop>
            <source src="/video/lemusee_drone.mp4"
            type="video/mp4">
            Désolé, votre navigateur ne prend pas en charge les vidéos intégrées.
        </video>
    {# </div> #}
</div>
<div class="container">
    <div class="jumbotron">
        <h1 class="home-caption">Le plaisir de l’exception avec le Musée</h1>
        <p class="lead">Vous rêvez d’une voiture d’exception, de revivre vos séries télévisées à travers les voitures qui ont marqué le siecle dernier ?</p>
        <hr class="my-4">
        <p>Venez rencontrer notre équipe de passionnés qui vous aideront à trouver ou à restaurer votre véhicule...</p>
        <p class="lead my-4">
        <a class="btn btn-primary btn-lg" href="{{ path('a_propos') }}" role="button">Lire la suite</a>
        </p>
    </div>
</div>

<div class="container mt-3 shadow-lg p-0 mb-5 bg-primary rounded">
        <div id="carouselExampleSlidesOnly" class="carousel slide carousel-fade" data-interval="3000" data-pause="false" data-ride="carousel">
            <div class="carousel-inner">
            {% for voiture in voitures %}
                {% set url = path('voitures_show',{'slug': voiture.slug}) %}
                    <div class="carousel-item {% if loop.first %} active {% endif %}">
                        <a href="{{ url }}" target="_blank">
                            <img src="/uploads/{{voiture.coverImage}}" class="img-fluid" style="height: 500px; width: 100%; display: block" alt="Responsive image" alt="...">
                        </a>
                        <div class="carousel-caption d-none d-md-block">
                            <a href="{{ url }}" target="_blank">
                                <h1 class="voiture-title">{{ voiture.marque }} {{ voiture.modele }} {% if voiture.type %} {{ voiture.type }} {% endif %}</h1>
                                <p>{{ voiture.etat }}</p>
                                <p>{{ voiture.prix }} prix TTC</p>
                            </a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

{% endblock %}
