<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="#">
            Newsletter
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown active">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Administration du site</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('admin_voitures_index') }}">Gestion des voitures<span class="sr-only">(current)</span></a>
                        <a class="dropdown-item" href="{{ path('admin_articles_index') }}">Gestion des articles<span class="sr-only">(current)</span></a>
                        <a class="dropdown-item" href="{{ path('admin_commentaires_index') }}">Gestion des commentaires<span class="sr-only">(current)</span></a>
                        <a class="dropdown-item" href="{{ path('admin_admins_index') }}">Gestion des administrateurs<span class="sr-only">(current)</span></a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_logout') }}">Déconnexion<span class="sr-only">(current)</span></a>
                </li>
            </ul>
        </div>
    </nav>
    
    {% for label, messages in app.flashes %}
        <div class="container">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                    <p>{{ message | raw }}</p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}